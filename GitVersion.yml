mode: ContinuousDeployment
continuous-delivery-fallback-tag: ''
branches:
  dev(elop)?(ment)?$:
    tag: beta
  features?[/-]:
    tag: alpha
  releases?[/-]:
    tag: RC
  master:
    tag: ''
ignore:
  sha: []

mode: ContinuousDeployment
branches:
  dev(elop)?(ment)?$:
    tag: beta
  features?[/-]:
    tag: alpha
  releases?[/-]:
    tag: RC
  master:
    tag: ''
    mode: ContinuousDelivery
ignore:
  sha: []